------------------------ESTRUTURA DA PÁGINA--------------------------

Páginal Inicial (Home) - requer login do usuário caso seja o primeiro acesso (não esteja salvo em cookies)

    - navbar: navegação no topo da página 

        -> Conta (config dados do usuario) 

            ->> Nome
            ->> Endereço
            ->> Telefone
            ->> E-mail
            ->> Items emprestados e data de entrega (destaque se está atrasado)
            ->> Histórico (itens já devolvidos)

        -> Empréstimos (cadastro, relatorios, emprestismos)

            ->> Lista de itens
            ->> Botão cadastrar novo emprestimo (abre formulario)
            ->> Botão gerar relatório (download do relatório dos empréstimos)

    - body: login do usuário (login e senha checa na base de dados e retorna)

    - footer: contato empresa, dúvidas frequentes


Features:
- Realizar o cadastro dos usuários e dos itens.
- Realizar o login do usuário.
- Permitir ao usuário acessar/configurar seus dados, que ficam salvos.
- Realizar o cadastro do item emprestado, data e contato do destinatário.
- Opcionalmente, pode haver a data de devolução combinada.
- Na tela inicial, mostrar lista dos itens emprestados e permitir clicar em botão para cadastrar um novo empréstimo.
- Após a data combinada, o item deve ficar destacado na lista de empréstimo; se não houver data determinada, deve-se destacar também.
- Quando algo for devolvido, deve ser marcado no sistema, que irá salvar a data.
- Opcionalmente, pode gerar relatórios de itens emprestados pendentes.


----------------------------TIPOS DE DADOS-------------------------------

Que tipo de objetos vamos emprestar?

Exemplo: Livro
    int id NOT NULL primary key
    String Nome NOT NULL
    String autor NOT NULL
    String ISBN NOT NULL
    int numeroPaginas 
    String genero NOT NULL

Exemplo: Ferramentas
    int id NOT NULL primary key
    String Nome NOT NULL
    String estado NOT NULL
    int voltagem

